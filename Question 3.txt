Scenario 4

a. The number of unpaid order vs paid in a month

//Query For Number of Paid Orders
SELECT count(id) FROM orders WHERE MONTH(created) = MONTH(CURRENT_DATE()) AND YEAR(created) = YEAR(CURRENT_DATE() AND status = paid

//Query For Number of Pending Orders
SELECT count(id) FROM orders WHERE MONTH(created) = MONTH(CURRENT_DATE()) AND YEAR(created) = YEAR(CURRENT_DATE() AND status = pending

b. Cost incurred to generate weekly sales

//Query for Cost Incurred to Generate Weekly Sales

c. Average daily sales

//Query for average daily sales
SELECT AVG(min_price) as average_daily_sales, created_at FROM auctions group by DATE(created_at)

d. The inventory movement based on Paid Orders and Unpaid Orders

//Query of inventory movement based on Paid Orders

//Query of inventory movement based on Unpaid Orders



